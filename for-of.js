// For of -> usado para fazer loop interaveis: interavel e qlq coisa que tenha um comprimento (array, string)

const test = "Leonardo";

// Mais utilizado para percorrer arrays

for (const letter of test) {
  console.log(letter);
}
